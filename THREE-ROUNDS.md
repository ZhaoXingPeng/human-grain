# 三轮设计记录

这不是把同一个 prompt 重复三次，而是三次有不同问题、不同产出的 loop。记录采用本地 `tool-note-and-vote` 的静默发散、投票、讨论、Decider 收敛结构。

记录元数据：2026-07-28；参与者：主 agent（设计/实现）+ 独立审查 agent；主持：主 agent；Decider：主 agent；以下票数是可审计的设计模拟，不是外部团队的真实投票。

## 第 1 轮：发散

决策问题：人为感应该主要靠哪些可控机制产生？

静默候选板（每项 1 票，Decider 可投 2 票）：

| 候选 | 票数 | 备注 |
| --- | ---: | --- |
| 去套话和句长变化 | 5 | 所有场景都需要 |
| 具体场景和作者判断 | 5 | 比泛泛“加个性”可靠 |
| 灵动 Markdown 动作库 | 4 | 用户硬性要求，需单独设计 |
| 少量安全错别字 | 3 | 只能是小预算，不能主导风格 |
| 随机打乱格式 | 0 | 不可回滚，也不可读 |
| 伪造个人经历 | 0 | 越界，直接淘汰 |

讨论结论：把“内容颗粒”和“格式颗粒”拆成两条轴；随机打乱和伪造经历不进入方案。

选择结果：把“内容噪声”和“格式噪声”拆成两个可调轴，并增加 `draft`、`rewrite`、`repair`、`variant`、`notes-to-doc` 五种入口。Decider 选择：Human Grain｜人间颗粒。

## 第 2 轮：约束

产品设计检查重点是：不改事实、不伪造经历、关键字段零错字、范围修复不能越界、粗糙度默认不超过 3/5。架构检查重点是：模式、噪声预算、格式策略和验证清单都必须成为可执行规则，而不是口号。

第二轮回归：增加输入契约、front matter/token snapshot、noise ledger 和格式动作预算；`repair` 缺范围时改为先确认。

## 第 3 轮：收敛

最终采用“三轮处理”：Detect/Strip → Voice/Friction → Format/Grain。每轮都产出可检查的变化；最终检查同时覆盖语义保真、Markdown 可解析性和人为颗粒感。

第三轮回归：用五个 fixture 回放五种入口，额外确认格式动作不是固定配方，并为粗糙度 3/5 以上加入可回滚 noise ledger。失败的早期方案是“每篇固定加一个引用块 + 一个错别字”，已删除。
