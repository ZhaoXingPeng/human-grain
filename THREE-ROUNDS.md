# 三次内部检查

这不是让模型把同一个 prompt 重复三遍，而是三个不同的问题：事实有没有被守住，结构有没有服务读者，最后的 Markdown 能不能继续用。

## 1. 抽锚点

找出人名、日期、数字、接口、代码、链接、限制和原作者的判断。删掉空话、重复背景和没有依据的承诺。

## 2. 重排留摩擦

先写读者要做的决定或动作。合并、删减、调换章节都可以；输入里的未知、失败、争议、补丁请求和下一步要留下。没有来源的个人经历不补。

## 3. 保护读回

核对 YAML、可变长度代码围栏、链接、脚注、表格、任务项、命令、数字和接口。`repair` 还要核对范围外内容。默认不把检查过程塞进正文。

早期固定“每篇一个引用块 + 一个错字 + 一个台账”的方案已删除。它只是换了模板。
