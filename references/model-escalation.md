# 模型升级与复核

Human Grain 的规则独立于模型。普通流程由一个主模型完成三次内部检查；高风险或用户觉得“还是太像 AI”时，可以追加一次独立复核。复核是可选的，不应阻塞主流程。

复核模型（例如环境中可用的 Hermes、最新长上下文模型或另一个供应商模型）只回答三件事：

- 哪些句子仍然像模板？
- 哪些格式变化看起来是机械随机的？
- 有没有事实、数字、引用或范围漂移？

复核模型不直接重写全文。主模型根据问题清单做最小改动，再跑最终检查。没有第二个模型时，用主模型的“反方审稿”提示完成同样的检查；不得因为某个模型名称不可用而等待或伪造调用结果。
