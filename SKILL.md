---
name: human-grain-writer
description: "中文优先的 Markdown 起草、改写、修补和变体 skill：按原文体裁、作者位置、协作状态和证据重写，允许不完整和不对称，但不靠口头禅、随机错字或装饰性格式制造人味；保护事实、接口、数字、链接、代码和 Markdown 语法。适用于 draft、rewrite、repair、variant、notes-to-doc 等请求，不用于规避审查、伪造经历或改写高风险事实。"
---

# Human Grain｜人间颗粒

这个 skill 不是“把 AI 句子改得更像聊天”。它把文档还原成一个具体人在某个时间点、拿着手头材料写下的工作件：知道的写清，不知道的留着，已经失败的不要抹掉，没发生过的不要补。

## 先读出原文的来路

改之前只做一张内部小地图，不输出模板化分析：

- 这是 RFC、README、接口契约、会议记要、Proposal、日报、复盘，还是别的东西？
- 作者是在提议、报告现状、求补丁、交付实现，还是给后来的人留线索？
- 文中有哪些可核对的人、日期、版本、数字、命令、接口、限制和失败？
- 文中哪些话是在协作：请谁回复、如何复现、需要什么机器信息、哪件事还没决定？

体裁先于“人味”。RFC 可以有定义和 open questions，README 可以直接告诉安装者哪里会失败，接口文档可以只说边界和参数。不要把它们统一改成轻松口语，也不要把同一种“真人腔”贴到每篇文档上。

用户没有指定入口时默认 `rewrite`。说“最大力度”“越粗越好”“实验稿”时才用 `extreme`；医疗、法律、财务、安全、代码、配置和公开承诺默认 `safe`。

## 入口

| 模式 | 处理 |
| --- | --- |
| `draft` | 从已有主题和要点写一份能继续接的稿；缺口写缺口，不编造背景。 |
| `rewrite` | 保事实锚点，重做入口、顺序和密度；不是逐句同义替换。 |
| `repair` | 只改指定范围；没有范围先停下来问，不默认重写全文。 |
| `variant` | 改读者入口和解释顺序，不增加能力、数据或承诺。 |
| `notes-to-doc` | 让碎片可读，但保留说话人、来源顺序和未决状态。 |
| `continue` / `outline-to-doc` / `status-log` | 接着原来的断点、扩写提纲或整理状态；不把半成品抛光成完成稿。 |

## 最大力度到底改什么

`extreme` 不是更会说俏皮话，而是敢动骨架。至少要发生一次真正的结构操作，具体是哪一种由材料决定：

- 把重复的背景和结论合并，删掉不产生判断的段落；
- 把调用边界、失败条件、安装前提或下一步提前，背景放到后面；
- 把一张“什么都想包含”的表拆成一段说明和一个短清单，或反过来；
- 把同一份材料里的提议、实现、问题和协作请求按原作者实际思路重新接起来；
- 保留一段很短的状态和一段很长的技术细节，让信息密度自然不均；
- 留下原文已经存在的“没做、没测、可能不行、请发复现”，不要替它收尾。

结构操作之后还要读起来像同一类文档。不能把架构说明改成聊天记录，不能把 RFC 改成散文，不能把 README 的安装步骤打散。`extreme` 允许粗糙，不允许失去用途。

以下句子和动作不能作为“去 AI 味”手段反复添加：`先说清楚它做什么`、`先记一下`、`这里故意`、`闭环以后再说`、`这份稿子不漂亮`、`别急着`，以及它们的同义变体。它们是表演，不是证据。

## 三次内部检查，不是三段输出

1. **保事实**：保存人名、日期、数字、因果、接口、字段、代码、命令、链接、引用和原文限制。发现冲突就标出冲突，不替人裁决。
2. **破骨架**：从上面的地图中选结构操作。长文按信息块重组；短文信息不足时，允许只删套话，不强行造“毛边”。
3. **读回**：检查事实、原文协作线索、结构用途和 Markdown 解析。默认只交正文；用户明确要审计时，另给简短差异和保护检查。

## 粗糙度不是坏字数

| 等级 | 工作状态 |
| --- | --- |
| 1 | 句子和段落基本稳定，只清理套话和重复。 |
| 2 | 局部改顺序，保留一个真实的缺口、失败或行动请求。 |
| 3 | 明显工作稿：有一处骨架重排，段落密度开始不均。 |
| 4 | 讨论稿/赶工稿：删并更多，未决、争议、复现要求和作者修正留在原处。 |
| 5 / `extreme` | 不再维护原来的章节对称性；可以少标题、长短段混用、局部跳转和半成品，但读者仍能完成实现、安装、评审或协作动作。 |

错别字默认关闭。只有用户明确要求 `typo_noise`，且材料不是高风险内容时，才允许极少量普通正文误写；不碰人名、数字、单位、字段、接口、代码、链接、标题关键词和结论。错字不能用来证明“像人”。

## 协作痕迹

早期技术文档的真实感常常来自协作，而不是语气：作者写姓名和地址，要求补丁格式，说明如何复现，把“还没实现”放在显眼处。保留输入里已有的这些东西。输入没有人名、经历或反馈时，不要凭空写“我见过”“大家都说”“用户会喜欢”；最多留下 `[待确认]` 或一条明确的资料缺口。

## 保护边界

- 不改事实、数字、因果、引用、接口语义、字段、代码、命令、链接、许可证和任务要求。
- 保护 YAML front matter、反引号和波浪号代码围栏（长度可变）、普通/参考链接及定义、图片 alt/destination、脚注引用及定义、HTML 注释、HTML table、Mermaid/LaTeX、任务复选框、有序步骤和表格语法。
- `repair` 范围外 token 不动；`safe` 不改变步骤、表格、代码、契约和 AST 顺序。
- 不通过故意错误规避审查，不伪造作者、来源、研究数据、用户故事或个人经历。

默认输出正文，不加模式行、三轮日志、噪声台账或格式台账。审计请求另说。需要体裁信号、早期样本和 Markdown 保护细节时，读取 `references/format-playbook.md`、`references/roughness.md`、`references/ai-tells.md`、`references/modes.md` 和 `references/pre-2019-corpus.md`。
