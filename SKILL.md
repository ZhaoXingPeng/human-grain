---
name: human-grain-writer
description: "去 AI 味、低文采、像个人写的 Markdown skill。Create or rewrite Chinese-first Markdown with flexible human roughness: remove AI patterns, vary structure, add bounded typos and lively formatting, while preserving meaning. Use for 人间颗粒 / Human Grain draft, rewrite, repair, variant, or notes-to-doc requests; do not use to evade review or alter high-risk facts."
license: MIT
metadata:
  version: "0.1.0"
  category: writing
  language: zh-CN
  brand: "Human Grain | 人间颗粒"
  language_scope: "Chinese-first; English gets structural rules, not a full bilingual typo lexicon"
---

# Human Grain｜人间颗粒

把文档写得像一个真实的人在有限时间里写出来的。重点不是把句子故意写坏，而是去掉机器的整齐、完整和过度解释，同时保留作者的意思、证据和基本可读性。

## 先选模式

从用户请求中选择一个模式，并在输出中记录模式、profile、粗糙度和 typo 开关，例如：`模式：repair｜profile：safe｜粗糙度：1/5｜typo_noise=off`。如果输入有 YAML front matter，必须原样保留在首行；模式记录放在 front matter 后，或放进文末处理记录，不能破坏 front matter。

| 模式 | 输入 | 输出 |
| --- | --- | --- |
| `draft` | 主题、要点或零散材料 | 一份从零开始的粗糙 Markdown 初稿 |
| `rewrite` | 一份完成度较高的文档 | 保留事实和意图的“人间颗粒版” |
| `repair` | 一份需要继续修改的文档 | 只改指定问题，并顺手留下少量自然痕迹 |
| `variant` | 原文 + 目标读者/场景 | 同一内容的另一份粗糙变体 |
| `notes-to-doc` | 会议记录、聊天片段、清单 | 把碎片整理成不太工整但能用的文档 |
| `continue`* | 未完稿 + 接续目标 | 接着写，保留原稿断点 |
| `outline-to-doc`* | 提纲 + 读者/场景 | 生成可继续编辑的工作稿 |
| `status-log`* | 日报、进度碎片、阻塞项 | 生成带未决项的状态记录 |

带 `*` 的是扩展入口，仍必须遵守同一套保护和三轮规则。

如果用户没有说明，默认使用 `rewrite`，粗糙度为 3/5，`typo_noise=off`。用户明确说“极致”“更粗”“实验稿”时，启用 `extreme` profile：内容和格式两条轴独立拉高；只有用户也要求错别字时才打开 `typo_noise=on`。两种 profile 都仍执行保护对象、事实边界和 noise ledger。

## 三轮处理（硬性）

每次必须循环三轮。不要把三轮合并成一句“已优化”。

### 第 1 轮：去机器味（Detect → Strip）

- 找出模板化开头、假平衡、总结复述、套话、过密的连接词和统一句长。
- 删除或改写 `综上所述`、`值得注意的是`、`在当今...背景下`、`赋能`、`打造闭环` 等空话。
- 保留事实、数字、限制条件和原作者真正的判断；不凭空补数据。
- 让段落长短不再平均。可以有一行段，也可以突然停一下。

### 第 2 轮：加人的东西（Voice → Friction）

- 只复用输入或用户上下文里已有的人、时间、动作和场景；输入没有这些信息时，不得凭空补人名或经历，改用无主语动作或 `[待补场景]` 占位。
- 允许自我修正、口头连接、半句停顿、轻微重复，例如“其实”“先说这个”“我这里也没想得特别完整”。
- 句式长短交替；不要每段都遵循“观点-解释-例子-总结”。
- 适量加入不影响理解的错别字、漏字或口语写法。只有 `typo_noise=on` 时启用，默认每 500 字 0-2 处，且不能出现在人名、数字、代码、链接、标题关键词和安全/法律结论中。
- “像人”优先来自具体场景、个人判断和局部摩擦，不是凭空添加励志话、情绪和所谓个人经历。没有来源就不写“我亲眼见过”。

### 第 3 轮：灵动改 Markdown（Format → Grain）

- 只使用能表达语气或阅读节奏的格式变化：单句段、短列表、临时小标题、括号旁注、引用块、断开的列表、一个不完整的表格。
- 不要随机打乱所有格式。格式变化必须有理由：强调、犹豫、插话、对照、暂存未决信息。
- 标题层级可以不完全对称；列表项目可以长短不一；但代码围栏、链接、表格分隔线和 HTML 标签必须可解析。
- 处理前先做一个轻量 token snapshot：YAML front matter、代码围栏、普通/引用式链接、脚注定义、HTML 注释、Mermaid/LaTeX 块、任务复选框、嵌套列表和有序步骤都列为保护对象。处理后逐项核对，保护对象不得被改写或丢失。
- 图片语法（alt 文本和 destination）、HTML table/ARIA 属性、YAML anchor/alias 也进入保护快照。
- 不跳级改标题、不打乱有序步骤、不拆散引用定义；“断开的列表”只表示内容粒度不对称，不表示缺少 Markdown 语法。
- 如果原文已经有成熟结构，优先保留结构，只在局部留下毛边。
- 先判断文本的来源形状（聊天、会议、说明、提案、日志）和阅读节奏，再选择 2-4 个格式动作；每次组合可以不同，不套固定配方。具体选择见 `references/format-playbook.md`。

## 粗糙度刻度

| 等级 | 内容 | 格式 | 错别字/不完整 |
| --- | --- | --- | --- |
| 1 | 只去套话，基本顺滑 | 原结构 | 0 |
| 2 | 口语化，句长有变化 | 少量单句段/旁注 | 偶发口语缩写 |
| 3 | 明显真人初稿感 | 标题和列表不完全对称 | 每 500 字 0-2 处 |
| 4 | 赶时间写的工作稿 | 局部断裂、临时标记、未决段落 | 每 500 字 2-4 处，避开关键内容 |
| 5 | 极端实验稿 | 结构明显毛边但仍能读 | 每 500 字最多 5 处；必须标注为实验输出 |

`extreme` profile 使用 5/5 的双轴预算：正文最多 5 处可回滚的安全噪声/500 字，格式动作 3-6 个且必须说明阅读目的。它可以更不对称、更像赶工工作稿，但不能破坏受保护 Markdown token，也不能改事实。

预算按 `eligible_body_chars` 计算：去掉 front matter、代码围栏、链接 URL、HTML/注释和表格语法后，按 Unicode 字符计数；短于 500 字时按 `ceil(chars / 500)` 取一个预算桶，正文零字符则预算为 0。格式动作按实际出现次数计，不按种类糊弄。

格式还要限制出现次数：每 500 字最多 2 个引用块、2 个临时标题、1 个临时表格；单句段和旁注合计不超过 5 处。每个动作的实际位置、理由和是否回滚写入 round-3 ledger，避免“动作种类少、重复次数很多”。

### safe profile

医疗、法律、财务、安全、代码、配置和对外承诺自动使用 `safe` profile：只做套话替换、句长调整和明确的语法修复；不加错别字、不加临时标记、不打断列表/表格/步骤，不改变 AST 顺序。用户必须明确确认后才能离开 safe profile。

没有用户明确要求时，不超过 3/5。涉及医疗、法律、财务、安全、学术证据、代码、配置和对外承诺时，错别字预算固定为 0，最多使用口语和结构变化。

### 噪声台账

粗糙度为 3/5 或以上时，内部保留一份 noise ledger；用户需要审计时再输出。每条记录包含：`原词 -> 变体`、轮次、位置、类型（漏字/近音/空格/口语缩写）和回滚理由。没有噪声时写 `无`。否定词、日期、金额、单位、人名、字段名、表格单元格、代码和链接永远不进入台账。

### 格式台账

第三轮至少保留一行：`格式台账：轮次 3｜动作｜位置｜目的｜可回滚：是/否`。如果是 safe profile 或短文 no-op，记录原因和“可回滚：是”。

## 内容与格式的安全边界

1. 不改变原文事实、数字、因果、引用、代码、命令、链接和任务要求。
2. 不伪造个人经历、来源、研究或引用来制造“真人感”。
3. 不通过错误拼写去规避审查、冒充他人或隐藏 AI 使用；需要时明确这是风格实验稿。
4. 先给出可用正文，再附一段很短的“处理记录”：模式、粗糙度、刻意保留的结构变化。
5. 用户说“只修某一段”时，其他段落不得顺手重写。
6. 如果用户要求实验级粗糙度，仍然保持链接、代码、表格语法和 front matter 可解析；“灵动”不等于随机破坏。
7. 格式动作有预算：默认 2-4 个种类，同时遵守出现次数上限；每个动作要能解释阅读目的，超过预算必须说明原因。
8. 极短输入（少于 120 个 eligible 字符或只有一个标题）允许第三轮 no-op，记录“信息不足，保留原结构”；长文（超过 3,000 个 eligible 字符）按标题分块处理，并在最后合并全局保护快照和噪声台账。

## 模型与复核

本 skill 不绑定某一个模型。优先使用当前环境中可用的高质量长上下文模型；如果工具列表中确实能发现 Hermes 或其他独立审稿模型，可把它放在第三轮之后做一次只读审查，检查事实漂移、模板化格式和过度人为痕迹，再由主模型决定是否采纳。如果工具不存在、调用失败或涉及敏感材料，跳过复核并继续本地检查，不等待、不阻塞交付。模型名称不能替代三轮规则，也不能用来伪造来源。

## 输出模板

```markdown
模式：rewrite｜profile：standard｜粗糙度：3/5｜typo_noise=off

# 文档标题（可以不那么完美）

正文……

> 这里有个没完全想好的点：……

## 先记一下

- 要点一
- 要点二（这个后面可能还要改）

处理记录：保留了原事实，打散了段落节奏，加入少量口语停顿；关键字段未做噪声处理。
格式台账：轮次 3｜单句段 + 旁注｜开头/转折｜调整阅读节奏｜可回滚：是
```

## 最终检查

- 读者能在开头 30 秒内知道文档要做什么。
- 文档不像统一模板，但仍然能被人读懂、复制、继续编辑。
- 三轮确实都发生过：去机器味、加人味、改格式。
- 关键事实没有被“粗糙化”改错。
- Markdown 结构至少能被常见渲染器正常解析。

需要更详细的模式示例、AI-tell 诊断、噪声词表、格式动作或模型复核时，读取 `references/modes.md`、`references/ai-tells.md`、`references/roughness.md`、`references/examples.md`、`references/format-playbook.md` 和 `references/model-escalation.md`。
