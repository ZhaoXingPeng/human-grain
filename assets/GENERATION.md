# Asset Provenance

封面和流程图的构图先按本地 `imagegen` / `frontend-design` 规则设计，再保存为仓库内的 SVG 源文件和 PNG 预览。

2026-07-28 曾按项目根目录 `生图.txt` 中的接口尝试生成位图，服务返回 `missing api key`，另一次请求超时；密钥没有写入脚本、日志或 Git 历史。为保证 README 在 GitHub 上有真实位图，当前 PNG 是由本仓库 SVG 源文件本地栅格化得到的可替换占位素材。接口恢复并提供有效凭证后，可直接替换 PNG，不改变 README 引用路径。

设计方向：深炭黑背景、灰白纸张、荧光绿标记和少量橙色校正点，表达“高端工具外壳 + 不整齐的人写痕迹”，避开默认的奶油色草图封面。
