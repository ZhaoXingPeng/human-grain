# 本地文档实测

这次测试直接读取当前工作区的三份 Markdown 原文件，没有改动它们，也没有把原文复制进公开仓库。第 1 轮用 `scripts/ai_tell_report.py` 做诊断；第 2、3 轮按 Human Grain 的运行规则对短摘录做人工回放，再检查保护对象和格式台账。

## 结果摘要

| 样本 | 模式/profile | 文件规模 | Round-1 AI-tell | 结果 |
| --- | --- | ---: | ---: | --- |
| GitHub 个人项目管理战略 | rewrite / 3/5 | 145 行，13,490 字节 | 0 | 结构可保留，适合重写 |
| 语音日程提醒战略 | variant / 3/5 | 307 行，24,761 字节 | 0 | 长文应按标题分块 |
| 语音服务设计 | repair / safe | 353 行，15,690 字节 | 0 | 技术块只做安全改写 |

`0` 不代表文档质量或“没有 AI”，只代表当前轻量词表没有命中；人的判断仍要看上下文。

## 01 / rewrite：战略文档

来源：本机样本 A（GitHub 个人项目管理战略，原文件留在工作区，不随仓库发布）

抽取到的原始意思：只做个人 GitHub 任务池，不扩团队协作；先确认目标用户、数据边界和三个 Proposal。

三轮回放摘录：

```markdown
# 这次先做什么

先别把它做成团队管理工具。现在只解决一个人的问题：打开以后，知道今天该先推哪个 Issue、哪个 PR。

GitHub 还是事实来源。工具只收和我有关的行动，排序可以建议，但最后由人决定。

## 先确认这几件事

- 用户是谁
- 只读哪些 GitHub 对象
- 三条 Proposal 怎么拆

其他方向先放着，日报、语音输入、长期倒排这次不进来。
```

Round-2 只复用了源文已有的用户、GitHub、Proposal 和不做项，没有新增姓名、数字或经历。Round-3 选择了单句段 + 不对称列表 + 临时小标题，动作台账为 3 种/3 处。

## 02 / variant：长战略文档

来源：本机样本 B（语音日程提醒战略，原文件留在工作区，不随仓库发布）

目标读者设为“第一次接手项目的组员”，场景是“在群里快速看懂当前 MVP 边界”。长文按标题分块，保留原有 Proposal、P1/P2/P3 和风险边界，不凭空加产品能力。

输出摘录：

```markdown
# 先看 MVP 到哪

这次不是做一个会替你安排一切的秘书。先把三件事做准：说一句，能建日程；到点，能提醒；要改，先问清楚改这一次还是以后都改。

P1 建立事项，P2 处理提醒，P3 改已有日程。顺序先这样，别再往外扩。

> 还没决定的入口载体，先留在问题清单里。
```

Round-3 没有把每个章节套成相同模板；只在读者真正需要快速扫读的地方放了一个引用块和一个短列表。

## 03 / repair + safe：技术文档

来源：本机样本 C（语音服务技术设计，原文件留在工作区，不随仓库发布）

修正范围：只改“服务定位”第一段的语气。由于文档含 C++、Mermaid、接口名和音频参数，自动降级为 `safe`：代码块、图、数字、调用方向和标题顺序保持不变。

允许的修改示例：

```markdown
语音服务是 VoiceLife 和用户之间的音频桥梁。它不处理业务逻辑，主要负责听、说、唤醒这三件事。
```

保护检查：Mermaid fence、C++ fence、`PlayTts(text)`、`16kHz`、`60ms` 和服务名 token 均保留；错别字噪声为 0；Format round 记录为 no-op。

## 实测结论

- 三份原文件均保持不变。
- 长文分块规则适用；技术文档的 `safe` 降级有效。
- 轻量 AI-tell 词表没有误报这些真实项目文档。
- 这不是外部模型基准测试；若要比较 Hermes 或其他模型，应在工具可发现时追加只读复核，并把结果另存为不含原文隐私的报告。
