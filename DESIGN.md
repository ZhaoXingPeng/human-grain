# Design Notes

一句话结论：Human Grain 的差异不在“写得更像人”这句口号，而在于把人为感拆成可选择、可回滚、可检查的内容和格式变化。

下一步动作：先用 `rewrite`、粗糙度 3/5 跑真实 Markdown；只有读者确认仍然太工整时，才升到 4/5 或 5/5。

## 设计取舍

### 为什么不是随机错别字

随机错误很快会变成另一种模板，也会破坏事实和可读性。错别字只是噪声预算中的一个很小部分，主要颗粒来自节奏、段落和作者的临时判断。

### 为什么格式要单独处理

Markdown 的格式本身会泄露生成方式。完全对称的标题、列表和表格会让内容像模板；但随机破坏格式又不可用。所以 Format → Grain 只允许有语义理由的非对称。

真实写作者不会每篇都用同一个旁注、同一个列表和同一个错别字。skill 先判断材料来源和未决程度，再从动作库选择 2-4 个动作；组合会变化，但保护对象和预算不变。

### 为什么要三轮

一次完成通常会把“去 AI 味”和“写得粗糙”混成互相抵消的动作。三轮把诊断、声音和格式拆开，每轮都能回看发生了什么。

## README 视觉方向

按本地 `frontend-design`、`ui-design-system` 和 `imagegen` 技能做了一个克制的开源工具首页：深炭黑 `#0d0f0e` 作为底，灰白 `#f4f1e8` 作为纸张，荧光绿 `#b8f04a` 表示标记，橙红 `#ff6b4a` 表示修正点，冷蓝 `#8fb8ff` 表示第二阶段。标题使用系统无衬线，正文保持 GitHub 原生阅读节奏；hero 只承担“编辑工作台”的记忆点，前后对照图承担产品解释，不堆装饰。

生图 API 调用记录见 [`assets/GENERATION.md`](assets/GENERATION.md)。接口凭证无效时没有伪造生成结果，仓库保留 SVG 源和 `1600×900`、`1600×360`、`1600×650` PNG 栅格预览，方便以后替换。发布前用本地 Quick Look 栅格化检查了尺寸和透明通道，README 同时保留 alt 文本。

## 非目标

- 不规避学术、平台或安全审查。
- 不伪造个人身份、经历、来源或引用。
- 不把代码、链接、数字和关键结论故意写错。
- 不追求每篇文档都像同一种“真人”。
