# Design Notes

结论：Human Grain 的粗糙度应该控制“作者在当时知道什么、没决定什么、读者先要什么”，而不是控制口头禅和错别字数量。最大力度改写应优先改变文档架构，最后才动句子表面。

下一步：每次先按原文体裁做一次完整重排；只有技术事实和 Markdown 保护检查通过，才交付更粗的版本。

## 主要取舍

### 不把错别字当引擎

早期公开项目的毛边来自 beta、pre-alpha、未完成协议、补丁请求和作者明确的“不知道”。随机错字很快会变成另一套格式，还会破坏字段和可读性，所以默认关闭。用户明确要求时也只在安全普通正文里少量使用。

### 不做固定动作库

单句段、列表、旁注、引用块都可以用，但不能每篇都出现。RFC、README、接口文档和会议笔记的格式理由不同；格式动作无法从材料解释时就不要用。

### 结构重排比改词更重要

把结论、调用边界、失败条件和下一步放到读者能找到的位置，通常比把“旨在”换成“要”更有效。`extreme` 允许删除冗余章节、合并重复说明、调换入口和保留不对称，但不允许改动事实锚点。

### 内部检查不等于输出日志

三次检查用于稳定质量：抽锚点、重排留摩擦、保护读回。对用户输出固定的模式行、噪声台账和格式台账，会把每篇文档重新变成模板；只有审计请求才附证据。

## 证据

`references/pre-2019-corpus.md` 记录了 20 个 1969—2005 的公开样本、抓取哈希和观察结果。共同点是具体、未完成、可协作；不存在一套可直接复制的“真人句式”。

## 非目标

- 不规避检测或审查。
- 不伪造作者、经历、来源、用户故事和数据。
- 不把技术设计文档改成聊天记录。
